# HRM 系统性能测试报告

## 1. 测试概述

| 项目 | 内容 |
|------|------|
| 测试对象 | HRM 人力资源管理系统后端 API |
| 测试时间 | 2026-06-07 20:49 |
| 测试工具 | Apache JMeter 5.6 |
| 测试环境 | 本地开发环境 (localhost:8888) |
| 后端框架 | Spring Boot + MyBatis-Plus + MySQL + Redis |

## 2. 测试目标

对 HRM 系统核心业务接口进行压力测试，验证系统在高并发场景下的：
- 接口响应时间
- 系统吞吐量
- 错误率
- 各接口性能瓶颈

## 3. 测试接口

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 员工列表查询 | GET | `/staff?current=1&size=10` | 分页查询员工，带权限校验 |
| 考勤列表查询 | GET | `/attendance?current=1&size=10` | 分页查询考勤记录 |
| 薪资列表查询 | GET | `/salary?current=1&size=10` | 分页查询薪资记录 |
| 首页员工统计 | GET | `/home/staff` | 首页仪表盘数据 |
| 首页计数统计 | GET | `/home/count` | 首页统计数据 |
| 首页部门统计 | GET | `/home/department` | 部门统计图表数据 |

## 4. 测试方案

### 4.1 并发策略

采用两阶段压测：

| 阶段 | 线程数 | 启动时间 | 持续时间 | 说明 |
|------|--------|----------|----------|------|
| 核心业务压测 | 50 | 5s | 30s | 模拟正常使用负载 |
| 峰值压测 | 100 | 3s | 15s | 模拟高峰访问 |

总并发线程：150，请求间隔：100-200ms（模拟用户思考时间）

### 4.2 请求模式

- 使用 **随机控制器** 打乱请求顺序，模拟真实用户行为
- 每个请求携带 JWT Token 进行认证
- 启用 Keep-Alive 连接复用

### 4.3 认证方式

通过 `/test/login` 接口获取 JWT Token（跳过验证码），所有业务请求在 Header 中携带：
```
Authorization: Bearer <token>
```

## 5. 测试结果

### 5.1 总体指标

| 指标 | 值 |
|------|-----|
| 总请求数 | 336 |
| 成功请求数 | 336 |
| 失败请求数 | 0 |
| **错误率** | **0%** |
| 总耗时 | 77 秒 |
| 平均吞吐量 | 4.4 req/s |
| 平均响应时间 | 23,051 ms |
| 中位数响应时间 | 6,373 ms |
| P95 响应时间 | 71,610 ms |
| P99 响应时间 | 72,964 ms |
| 最小响应时间 | 40 ms |
| 最大响应时间 | 73,408 ms |
| 接收数据速率 | 26.07 KB/s |
| 发送数据速率 | 1.39 KB/s |

### 5.2 各接口性能

| 接口 | 请求次数 | 平均响应(ms) | 中位数(ms) | P95(ms) | P99(ms) | 最小(ms) | 最大(ms) | 吞吐量(req/s) | 性能评级 |
|------|----------|-------------|-----------|---------|---------|----------|----------|--------------|----------|
| 首页员工统计 | 48 | 1,152 | 1,175 | 2,286 | 2,641 | 40 | 2,641 | 2.47 | 优秀 |
| 首页计数统计 | 46 | 2,802 | 3,001 | 4,124 | 4,198 | 165 | 4,198 | 2.37 | 良好 |
| 员工列表查询 | 46 | 5,144 | 5,673 | 7,142 | 7,313 | 666 | 7,313 | 2.48 | 一般 |
| 首页部门统计 | 51 | 5,260 | 5,696 | 6,836 | 7,146 | 461 | 7,146 | 2.87 | 一般 |
| 薪资列表查询 | 72 | 29,101 | 29,576 | 31,460 | 31,985 | 23,263 | 31,985 | 1.66 | 较差 |
| 考勤列表查询 | 73 | 67,958 | 69,399 | 72,924 | 73,408 | 58,358 | 73,408 | 0.96 | 很差 |

### 5.3 性能分布图

```
响应时间 (ms)
    |
75k |                                          ████ 考勤 (67,958ms)
    |                                          ████
50k |                                     ████ ████
    |                                     ████ ████
25k |                                ████ ████ ████ 薪资 (29,101ms)
    |                                ████ ████ ████
10k |                           ████ ████ ████ ████
    |              ████    ████ ████ ████ ████ ████
 5k |         ████ ████    ████ ████ ████ ████ ████ 员工 (5,144ms)
    |    ████ ████ ████ ██ ████ ████ ████ ████ ████ 部门 (5,260ms)
 1k |    ████ ████ ████ ██ ████ ████ ████ ████ ████ 首页员工 (1,152ms)
    |    ████ ████ ████ ██ ████ ████ ████ ████ ████ 首页计数 (2,802ms)
 0  +----+----+----+----+----+----+----+----+----+---
       首页   首页   员工  首页   薪资   考勤
       员工   计数   列表  部门   列表   列表
```

## 6. 问题分析

### 6.1 考勤列表查询严重缓慢

**现象**：平均响应 67.9 秒，最低 58 秒

**可能原因**：
1. SQL 查询未优化，可能存在全表扫描
2. 考勤表数据量大，缺少索引
3. 分页查询使用了 `OFFSET` 深分页

**建议**：
- 检查 `AttendanceMapper.xml` 中的 SQL 语句
- 添加 `(staff_id, month)` 联合索引
- 考虑使用游标分页替代 OFFSET

### 6.2 薪资列表查询较慢

**现象**：平均响应 29 秒

**可能原因**：
1. 薪资查询涉及多表 JOIN
2. 月度筛选条件未命中索引

**建议**：
- 优化 SQL，减少不必要的 JOIN
- 添加 `(staff_id, month)` 索引

### 6.3 首页接口表现良好

**现象**：首页三个接口响应时间在 1-5 秒

**分析**：
- 首页数据可能有 Redis 缓存
- 查询逻辑简单，无复杂 JOIN

## 7. JProfiler 分析指南

基于 JMeter 测试结果，考勤和薪资接口存在严重性能瓶颈。以下是使用 JProfiler 定位问题的方法。

### 7.1 连接应用

**方式一：启动时注入 Agent**
```bash
java -agentpath:/path/to/jprofiler/bin/linux-x64/libjprofilerti.so=port=8844 \
     -jar target/hrm-*.jar
```

**方式二：Attach 到运行中的进程**
```bash
# 找到进程 ID
jps -l | grep hrm

# JProfiler GUI → Attach to JVM → 选择进程
```

### 7.2 重点分析视图

| 视图 | 分析目标 | 关注指标 |
|------|----------|----------|
| **CPU > Call Tree** | 考勤/薪资接口的方法调用链 | 方法耗时、调用次数 |
| **CPU > Hot Spots** | 最耗 CPU 的方法 | 自身耗时占比 |
| **JDBC > JDBC Queries** | SQL 执行情况 | 查询耗时、执行次数 |
| **JDBC > JDBC Statistics** | 数据库连接池状态 | 活跃连接、等待时间 |
| **Memory > Allocation** | 对象分配热点 | 大对象、频繁分配 |
| **Threads > Thread Monitor** | 线程状态 | 锁等待、阻塞 |

### 7.3 预期瓶颈定位

根据 JMeter 结果推测：

**考勤接口（68秒）**
```
可能的调用链：
AttendanceController.list()
  → AttendanceService.list()
    → AttendanceMapper.selectPage()  ← 疑似瓶颈
      → SQL: SELECT * FROM sys_attendance 
             WHERE ... ORDER BY ... LIMIT 10 OFFSET N  ← 深分页
```

**薪资接口（29秒）**
```
可能的调用链：
SalaryController.list()
  → SalaryService.list()
    → SalaryMapper.selectPage()  ← 疑似瓶颈
      → SQL: 多表 JOIN + 月份筛选  ← 缺少索引
```

### 7.4 分析步骤

1. **启动 JProfiler，连接到 HRM 应用**

2. **在 JMeter 压测期间录制**
   - 开始 Recording
   - 运行 JMeter 测试
   - 停止 Recording

3. **查看 JDBC Queries**
   - 按耗时排序
   - 找到考勤/薪资相关的慢 SQL
   - 记录 SQL 语句和执行时间

4. **查看 Call Tree**
   - 展开 `AttendanceController.list()` 调用链
   - 找到耗时最长的方法
   - 确认是 SQL 问题还是代码逻辑问题

5. **查看 Memory**
   - 检查是否有大量对象未释放
   - 关注 ResultSet、List 等集合对象

### 7.5 JProfiler 快照分析

如果已录制快照文件（`.jps`），可通过以下方式分析：

```bash
# 命令行导出报告
jprofiler -a export -f snapshot.jps -o report.html \
  -config cpu,method,jdbc,memory
```

### 7.6 常见优化方向

| 问题类型 | JProfiler 表现 | 优化方案 |
|----------|---------------|----------|
| 慢 SQL | JDBC Queries 中耗时 >1s | 添加索引、优化 SQL |
| N+1 查询 | JDBC Queries 数量异常多 | 使用 JOIN 或批量查询 |
| 连接池耗尽 | JDBC Statistics 等待时间长 | 增大连接池 |
| 内存泄漏 | Memory 持续增长不释放 | 检查资源关闭 |
| 锁竞争 | Threads 中大量 BLOCKED | 优化锁粒度 |

---

## 8. 优化建议

### 8.1 数据库优化

```sql
-- 考勤表索引
ALTER TABLE sys_attendance ADD INDEX idx_staff_month (staff_id, month);

-- 薪资表索引
ALTER TABLE sys_salary ADD INDEX idx_staff_month (staff_id, month);
```

### 8.2 查询优化

1. 避免 `SELECT *`，只查必要字段
2. 分页查询使用游标分页：
   ```sql
   -- 替代 OFFSET
   SELECT * FROM table WHERE id > #{lastId} LIMIT 10
   ```

### 8.3 缓存优化

- 首页统计数据已走缓存，保持现状
- 考勤/薪资列表可增加短期缓存（5 分钟）

### 8.4 连接池优化

```yaml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
```

## 9. 测试结论

| 评估项 | 结果 | 说明 |
|--------|------|------|
| 系统稳定性 | 通过 | 150 并发下无报错 |
| 接口响应时间 | 不通过 | 考勤/薪资接口严重超时 |
| 吞吐量 | 偏低 | 4.4 req/s，需优化 |
| 错误率 | 通过 | 0% |

**总体评价**：系统在高并发下稳定性良好，但考勤和薪资查询接口存在严重性能瓶颈，需要优先优化 SQL 查询和索引。

---

*测试执行：JMeter 自动化*
*报告生成：2026-06-07*
